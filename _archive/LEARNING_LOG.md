# Not the real file — see /Claude best Practice manual/LEARNING_LOG.md instead

Created here by mistake on 2026-08-17 before checking [[rick-learning-goals]], which already
pointed at the correct location: `~/Claude best Practice manual/LEARNING_LOG.md` (a separate
connected folder, not this repo, and not under git). The real 2026-08-17 "Identity/Auth upgrade"
entry was written there instead.

This file and its matching `.command` script are safe to delete — the sandbox that created them
can't delete them itself (same read-only-delete trap as `sandbox-git-lock-trap.md`). Both are
untracked (never committed, never staged by any COMMIT script), so leaving them a while does no
harm either.
