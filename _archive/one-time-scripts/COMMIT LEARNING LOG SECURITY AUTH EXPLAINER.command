#!/bin/bash
# THIS SCRIPT IS A MISTAKE — created 2026-08-17 before checking the real LEARNING_LOG.md location.
# The real file lives in a separate folder: ~/Claude best Practice manual/LEARNING_LOG.md
# (not this repo, not under git). Nothing here needs committing. Safe to delete this file and
# docs/LEARNING_LOG.md — the sandbox that created them couldn't delete them itself.
echo "This script does nothing on purpose — see the comment at the top of this file."
read -n 1 -s -r -p "Press any key to close..."
