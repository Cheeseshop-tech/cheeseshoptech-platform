#!/bin/bash
echo "This script is superseded — the fix grew into a wider rollout across CRM, campaigns,"
echo "pricing and media (same read-failure-masking pattern, found in several other files)."
echo "Use \"COMMIT READ-FAILURE MASKING FIX ROLLOUT.command\" instead — it covers everything,"
echo "including the original CRM-05 fix. (Sandbox quirk: this stub couldn't delete itself,"
echo "so it's left here as a harmless no-op instead.)"
read -n 1 -s -r -p "Press any key to close…"
