# Cut & Wrap thumbnails — REFERENCE ONLY, DO NOT UPLOAD

Extracted from `MT Assortment Cut nWrap FW.pdf` (2026-07-09).

Every image here is a screen thumbnail: 116×111 px at the small end, 331×210 px at the large
end, 150–211 ppi. Existing Cloudinary packshots run 2000–6732 px. **These are not packshots**
and must not be uploaded to `monti-trentini/*` — a wrong image in the Media Hub is worse than a
missing one, because nothing flags it.

They are kept so we can (a) confirm which product each C&W item actually is, and (b) hand
Stefano a visual checklist when requesting hi-res originals.

Notes:
- `03044` and `03047` are the **same photo** in the source PDF. One of them is wrong.
- `01174-wedge` and `01174-disc` are two different products sharing one item number.
- `UNNUMBERED-*` files have no item number on the sheet at all.
